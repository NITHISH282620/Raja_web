# Playwright is a devDependency used only by scripts/inspect.mjs and
# scripts/check-a11y-fallbacks.mjs, which never run in a deploy. Without this
# its postinstall downloads ~300MB of browsers on every Vercel build.
playwright_skip_browser_download=1
PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1
